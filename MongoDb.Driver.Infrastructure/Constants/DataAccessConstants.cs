namespace MongoDb.Driver.Infrastructure.Constants;

internal static class DataAccessConstants
{
    internal const string MongoConn = "MONGODB_CONN";

    // database name to connect to
    internal const string MongoDatabase = "samples";

    // name of the collection containing records
    internal const string MongoCollection = "restuarants";
}
